name: Terraform Plan

on:
  workflow_dispatch

jobs:
  call-reusable-workflow:
    uses: rsarles/terraform-cicd-pipeline/.github/workflows/terraform-plan.yml@main
    with: 
      repo_name: ${{ github.repository }}
    secrets:
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}